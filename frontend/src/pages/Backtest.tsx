export default function Backtest() {
  return (
    <section className="page">
      <div className="page-heading">
        <span className="eyebrow">MINIMATCH</span>
        <h1>Backtest</h1>
      </div>

      <div className="panel">
        Backtest subsystem UI coming next.
      </div>
    </section>
  );
}
